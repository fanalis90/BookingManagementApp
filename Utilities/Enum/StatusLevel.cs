namespace BookingManagementApp.Utilities.Enum
{
    //membuat enum status level
    public enum StatusLevel
    {
        OnGoing,
        Completed,
        Rejected,
        Canceled,
        Requested
    }
}
